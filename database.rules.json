{
  "rules": {
    ".read": false,
    ".write": false,
    "presence": {
      "$uid": {
        ".read": "auth != null",
        ".write": "auth != null && auth.uid === $uid",
        ".validate": "newData.hasOnly(['connections', 'lastOnline'])",
        "connections": {
          "$connectionId": {
            ".validate": "newData.val() === true"
          }
        },
        "lastOnline": {
          ".validate": "newData.isNumber()"
        }
      }
    }
  }
}
